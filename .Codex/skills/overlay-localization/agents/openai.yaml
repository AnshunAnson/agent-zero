interface:
  display_name: "Overlay Localization"
  short_description: "Generic translation workflow with export/apply entry scripts"
  default_prompt: "Use $overlay-localization to scaffold a translation workflow with one export script and one apply script, choosing between overlay mode and existing locale-file mode."
